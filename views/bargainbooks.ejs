<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        .bargain-badge {
            background: #e74c3c;
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 15px;
            font-size: 0.8rem;
            margin-left: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>💰 Bargain Books</h1>
            <p>Amazing deals under £20 - <%= totalBargainBooks %> books found</p>
        </header>
        
        <nav class="nav">
            <ul>
                <li><a href="/">🏠 Home</a></li>
                <li><a href="/books/list">📖 All Books</a></li>
                <li><a href="/books/addbook">➕ Add Book</a></li>
                <li><a href="/books/bargainbooks">💰 Bargain Books</a></li>
                <li><a href="/books/search">🔍 Search Books</a></li>
            </ul>
        </nav>
        
        <main class="content">
            <div class="summary">
                <p>Found <strong><%= totalBargainBooks %></strong> books priced under £20</p>
            </div>
            
            <% if (bargainBooks.length > 0) { %>
                <table class="books-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Book Title</th>
                            <th>Price <span class="bargain-badge">UNDER £20</span></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% bargainBooks.forEach(function(book) { %>
                            <tr>
                                <td><%= book.id %></td>
                                <td><%= book.name %></td>
                                <td class="price">£<%= book.price.toFixed(2) %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <div style="text-align: center; padding: 2rem;">
                    <h3>No bargain books found</h3>
                    <p>All our books are premium priced. Check back later for deals!</p>
                </div>
            <% } %>
        </main>
    </div>
</body>
</html>